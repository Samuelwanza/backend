'use strict';

/**
 * trust-fund-country service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::trust-fund-country.trust-fund-country');
