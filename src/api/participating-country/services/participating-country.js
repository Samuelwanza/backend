'use strict';

/**
 * participating-country service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::participating-country.participating-country');
