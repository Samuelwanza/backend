'use strict';

/**
 * escrow-account-transaction service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::escrow-account-transaction.escrow-account-transaction');
