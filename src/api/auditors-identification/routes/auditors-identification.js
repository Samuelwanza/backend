'use strict';

/**
 * auditors-identification router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::auditors-identification.auditors-identification');
