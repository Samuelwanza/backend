'use strict';

/**
 * auditors-identification service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::auditors-identification.auditors-identification');
