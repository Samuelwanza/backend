'use strict';

/**
 *  auditors-identification controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::auditors-identification.auditors-identification');
